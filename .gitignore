# dependencies
node_modules/

# build output — regenerated by `npm run build`, never hand-edited
# dist/

# secrets — never committed, ever
.env
.env.*
!.env.example

# local cache — this is the file-based "state" from the architecture,
# fine to exist locally but shouldn't be committed
.backline-cache/

# editor/OS noise
.DS_Store
*.log

# Internal reference docs
PUBLISHING.md
